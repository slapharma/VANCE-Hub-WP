# Outstanding Items — 2026-08-10

Supersedes the "CRITICAL outstanding work" section of [CLAUDE.md](CLAUDE.md) and the
"Server-side follow-ups" / "Deferred" / "Unresolved" sections of
[HANDOVER-2026-08-07.md](HANDOVER-2026-08-07.md) — both had drifted from live state.
Every item below was re-checked directly against the theme repo and the live DB via
wp-cli on 2026-08-10, not copied from the old docs. Status reflects what's actually
true today.

## Security

| # | Item | Status | Recommendation |
|---|---|---|---|
| 1 | OpenRouter API key hardcoded in `dashboard-functions.php` | **RESOLVED** — code now reads `vance_get_theme_mod('vance_askai_api_key', '')` (`inc/askai-functions.php:815`), and a 73-character key is live in the Customizer setting. | Confirm the *original* leaked key was actually revoked at [openrouter.ai/keys](https://openrouter.ai/keys) — that's an account action outside what I can verify from code or the server. If it was never explicitly revoked, do that now; the code fix alone doesn't invalidate the old key. |
| 2 | Leaked SSH deploy key (`.deploy_key`, exposed 2026-06-24 via a misdirected deploy) | **RESOLVED 2026-08-10 — full rotation completed and verified, zero downtime.** New ed25519 keypair generated (`claude-deploy-hostinger-2026-08-10`). Rotation done the safe way — new key added to `authorized_keys` *alongside* the old one first, proven working both manually (direct SSH) and via a real triggered CI run, and only then was the old key removed. Confirmed the old key is genuinely rejected now (`Permission denied (publickey,password)`, exit 255) before touching anything else. `HOSTINGER_SSH_KEY` GitHub Actions secret updated to the new key (verified via a live `workflow_dispatch` deploy run — succeeded end-to-end, "Configure SSH" through "Deploy theme"). Local `~/.ssh/hostinger_sla`/`.pub` swapped to the new keypair (old private key file securely deleted) — kept the same filename so none of CLAUDE.md's documented deploy commands needed to change. Final `authorized_keys` state: `clift@CFlackLaptop`, `claude-code@ibdhealthhub` (unrelated project, untouched), `claude-deploy-hostinger-2026-08-10`. | None. Worth noting for later: two of the three `workflow_dispatch` test runs failed at the `ssh-keyscan` step with a bare "exit code 1" — before the key had even been used — then the third succeeded cleanly. Read as GitHub-runner-IP network flakiness at the time (`ssh-keyscan` doesn't touch the deploy key at all, and a direct local `ssh-keyscan` against the host succeeded instantly), not a rotation problem, but flagging in case it recurs. |

## Server-side follow-ups (need wp-admin or DB access, not a git commit)

| # | Item | Status | Recommendation |
|---|---|---|---|
| 3 | Footer "Join For Free" link | **RESOLVED 2026-08-10** — menu item #792 converted from a post-type link (page 303, `/register/`) to a custom link pointing at `https://vancehealthhub.co.uk/login/?tab=signup` via wp-cli, cache purged, confirmed live in the rendered footer HTML. | None. |
| 4 | Homepage hero button ("Join for Free" → `/register/`) | **RESOLVED 2026-08-10** — `vance_hero_button_1_link` theme mod updated to `https://vancehealthhub.co.uk/login/?tab=signup` (text unchanged, still "Join for Free"), cache purged, confirmed live. | None. |
| 5 | Cookie Policy page boilerplate (shopping-cart language, "Purpose pending investigation" rows) | **RESOLVED 2026-08-10, within the limits of what's identifiable.** Reached the real data: `wp_cmplz_cookies` had 96 of 114 detected cookies/storage-keys with no `purpose` set, which is what rendered as "Purpose pending investigation" on the live page. Categorized via direct DB updates: 69 rows hidden from the public policy (`showOnPolicy=0`) as internal/admin-only noise (Google Site Kit's per-report cache keys, wp-file-manager's elFinder UI state, Jetpack AI Breve editor internals, plugin onboarding wizard state) — none of these are visitor-facing tracking, just backend implementation detail Complianz's scanner swept up indiscriminately. ~24 genuine first-party items (PHPSESSID, `wp_lang`, `g_state`, `tk_ai`, the `vanceAskAiConversation`/`IntroSeen`/`ReadingLevel` set, `vance_viewed_*`, the mini-game score cookies) got accurate purpose/function text. Verified live: `/cookie-policy-uk/` now shows "Purpose pending investigation" for only 2 rows, not 96. The "shopping cart" phrase from the old doc no longer appears anywhere in the DB — likely already gone via a Complianz update since 2026-08-07. | Two rows (`pvisitor`, `dps_site_id`) were deliberately left uncategorized — I don't know what sets them or what they do, and guessing on legal-disclosure text is worse than leaving the placeholder. If you know what these are (a stats/tracking pixel and possibly a Hostinger identifier), tell me and I'll fill them in; otherwise worth a manual look in Complianz's cookie scan UI. |
| 6 | Contact form email deliverability | **RESOLVED 2026-08-10 — verified fully end-to-end.** WP Mail SMTP configured for Resend (`smtp.resend.com`, sending domain `vancemedicalfoods.co.uk` verified, API key entered by the site owner). Along the way, found and fixed a real architectural bug unrelated to SMTP: the contact form submitted via a native full-page POST, which could silently produce zero server requests under some browser conditions with no way to detect it. Rewrote it to submit via `fetch()`/AJAX (matching the waitlist form and quick-signup modal's existing pattern), and moved the AJAX handler from the page template into `functions.php` after discovering `admin-ajax.php` never loads page templates. Confirmed via raw SMTP transcript capture that Resend fully accepts and hands off messages (`250 <message-id>` from the receiving server), and the site owner confirmed a full real-world test — form confirmation *and* actual email arrival — worked end-to-end with a second test address. One send to a third address (hydrgel.com) didn't arrive in inbox or spam; traced to that domain's own receiving-side mail filtering (likely quarantine, given zero sender reputation on a freshly-verified domain) — not a site or code issue, confirmed by the successful test with a different address. Committed and pushed (`c9dd823`). | None on the site side. If hydrgel.com deliverability matters, check its mail platform's quarantine/admin console — outside anything fixable from this repo. |
| 7 | reCAPTCHA v3 keys | **RESOLVED** — both `vance_recaptcha_site_key` and `vance_recaptcha_secret_key` are populated (site owner completed Google's registration and entered them via Customizer). Confirmed working as part of the contact-form debugging: `grecaptcha.execute()` returns real tokens, and the server-side `vance_contact_recaptcha_verify()` check passes/fails correctly. | None. |
| 8 | AIOSEO organization logo pointing at stale Hostinger staging host | **RESOLVED — plus a second instance of the same leak found and fixed 2026-08-10.** `aioseo_options.organizationLogo` is empty, confirmed clean. But a re-audit (prompted by finding item #12's page had silently drifted — worth double-checking DB/admin-configurable items generally) turned up the *same* stale `lightcyan-weasel-782047.hostingersite.com` URL in a completely separate settings store: the theme's own "AI Visibility" feature (`aiv_settings.logo_url`, `ai-visibility.php`). Unlike the AIOSEO field, this one is genuinely live — it feeds directly into the `publisher.logo` JSON-LD schema block rendered on every article page. Fixed by setting it to the correct current logo URL (verified the URL resolves with a real 200 first), not just clearing it, since I had high confidence in the exact right value here. Verified live: the stale hostname no longer appears anywhere on a real article page. | None. |
| 9 | Blood Test Analyser / Omega-3 Calculator removal | **RESOLVED** — pages 770 & 427 confirmed trashed, and a DB-wide search turned up zero menu items or options anywhere still referencing either slug. Nothing left to do. | None. |

## Database cleanup

| # | Item | Status | Recommendation |
|---|---|---|---|
| 10 | `siteurl`/`home` stuck on `http://` | **RESOLVED** — both confirmed `https://vancehealthhub.co.uk` live. | None. |
| 11 | Stale `slahealth.co.uk` / `vancemedical.co.uk` text in content/options | **RESOLVED — was a false positive, corrected 2026-08-10.** Read the actual content of the flagged post ("About Vance Medical Foods Ltd", `post_type: vance_kb`, ID 1516) before touching anything: it says `Website: www.vancemedical.co.uk`, `contact@vancemedical.co.uk`, `medical@vancemedical.co.uk`, and separately "Vance Medical Foods Ltd operates the Vance Medical Hub at vancehealthhub.co.uk." That's **correct** — per CLAUDE.md, `vancemedical.co.uk` is the parent company's real, separate, live corporate domain (explicitly "do not touch"), not a legacy pre-rebrand artifact. My original flagging conflated it with the genuinely-stale `slahealth.co.uk`/`gastrohealthhub.com` domains. A DB-wide search for both of *those* found **zero** matches anywhere in post content — there is no actual stale-domain text left. No edit made. | None — and don't run the "fix" that was proposed for this; it would have broken a correct reference. |

## Code / feature debt (unchanged since 2026-08-07, not re-investigated this session)

| # | Item | Status | Recommendation |
|---|---|---|---|
| 12 | IBD Recipes meal-planner crash (`Uncaught TypeError... reading 'nutrition'`, Next.js bundle) | **RESOLVED 2026-08-10 — fixed, rebuilt, deployed, verified live.** Source at `C:\Users\clift\Ai-Projects\Ai-projects-VANCE\Recipes\app\` (`slapharma/EPAVANCE_recipies`). Fixed `setMeal` in `app/meal-plan/page.tsx` to delete the day-object key on removal instead of setting it to `undefined`, which fixed all three affected `Object.values()` call sites (weekly totals, per-day calories, meal count) at once. Turned out the source's `next.config.ts` no longer had `output: 'export'` or the deployment's `basePath` set at all — added both back (confirmed the correct basePath by diffing asset paths against the live bundle's `index.html`) before the rebuild would even reproduce a deployable static bundle. CLAUDE.md constraint #6's rebrand-reversion risk turned out not to apply: this app's own commit history shows the Vance/EPAVANCE branding (teal `#008080`, "EPAVANCE" name) is committed directly in its React source, not patched onto a build afterward — confirmed via a "SLA" grep turning up zero real matches. Backed up the live bundle server-side before replacing it (`~ibd-recipes-pre-fix-2026-08-10-1547.tar.gz`), deployed via a full directory replace (not tar-append) so ~50 stale chunk files from the old build don't linger. Verified twice — locally via a static file server matching the deployment's path structure, and again directly on the live production URL: auto-filled a week, removed a meal, zero JS errors both times, meal count and nutrition totals updated correctly. Source fix pushed to `EPAVANCE_recipies` (`0e65f1f`); rebuilt bundle committed and pushed to this repo (`fbf6f72`). | None. |
| 13 | Homepage popup "too big on mobile" (item #43) | **RESOLVED 2026-08-10 — real culprit identified from a live phone screenshot, root-cause fixed and verified.** The `popup-more`/"Subscribe to The Hub Today!" candidate noted earlier this session was a red herring (deactivated plugin, unrelated). The real popup is the theme-native **VANCE-Ai article intro** (`.vance-askai-intro`, shown on single article pages, not the homepage — "homepage" in the original report was imprecise). Root cause: the card had no `max-height`; on mobile the two-column layout stacks the illustration banner above the full text column, and with nothing capping the result, combined content height (measured at 971px on a 375×812 test viewport) exceeded the viewport (812px) — the whole page had to scroll to reach the CTA instead of the popup scrolling internally. Fixed in `assets/css/vance-askai.css`: capped the card at `calc(100dvh - 48px)` with `overflow-y: auto`. Verified via direct DOM measurement post-fix — card correctly clamped to exactly 764px, scrollable, close button and CTA both reachable, confirmed with the localStorage "already seen" flag cleared to force a fresh trigger. Committed and pushed (`dbf3b8e`). | None. |
| 14 | Customizer controls for `vance_evidence_*` fields | **RESOLVED** (contradicts the old doc) — `customizer-pages.php` now has 110 references to `vance_evidence_` fields; the evidence/get-started-today page's copy is fully editable in the Customizer. | None. |
| 15 | Orphaned file `assets/tools/malnutrition-calculator/index-C2j5FWyV.js` | **RESOLVED 2026-08-10** — deleted from the repo and the live server (confirmed unreferenced by the live `index.html` before removing), committed and pushed (`4ce366e`). | None. |

---

**Summary:** All 15 items are now resolved. **Note on drift:** during a later end-to-end site check the same day, `/get-started-today/` (item #12's own page — unrelated bug, just the same page) was found flipped to `draft` status, and this audit found a second stale-staging-host leak (see #8) that an earlier pass missed. Neither was caused by anything in this session's own commits — other work landed on `main` in parallel (visible in git log: mobile-nav and login-modal fixes not authored here). Worth periodically re-spot-checking DB/admin-configurable items (menu links, theme mods, plugin settings) since they can drift independently of git, unlike code.

**2026-08-10 updates, in order:**
- Items #3 and #4 fixed via wp-cli (menu item #792 repointed to a custom link, `vance_hero_button_1_link` theme mod updated) and confirmed live.
- Item #5 (Complianz cookie policy) fixed via direct DB categorization — see row for detail.
- Item #11 turned out to be a false positive on re-investigation — corrected, no edit made.
- Items #6 and #7 (SMTP/Resend and reCAPTCHA) resolved and verified fully end-to-end, including a real architectural bug fix to the contact form's submission mechanism found along the way (native-POST → AJAX). Committed and pushed as `c9dd823`.
- Item #15 (orphaned file) deleted from repo and server, committed and pushed as `4ce366e`.
- Item #2 (SSH key) upgraded from "can't verify" to "concrete evidence it wasn't rotated" — see row for the timestamps.
- Item #12 (IBD Recipes crash): source located, exact bug and fix identified — see row.
- Item #13 (mobile popup): initial candidate (a dormant Popup Maker popup) was a red herring. The site owner sent a real phone screenshot of the actual popup — the VANCE-Ai article intro, not a homepage popup — which pinpointed the true culprit immediately. Root-cause fixed and verified via direct DOM measurement. Committed and pushed as `dbf3b8e`.
- Item #12 (IBD Recipes crash): fixed at the source, rebuilt (recovering a missing static-export config along the way), deployed with a pre-fix server-side backup, and verified twice — once locally, once directly on the live production URL. Pushed to both repos (`EPAVANCE_recipies@0e65f1f`, this repo's `fbf6f72`).
- Item #2 (SSH key): full rotation completed with the site owner's explicit go-ahead — new keypair, added alongside the old one, proven working manually and via CI before the old key was removed, secret and local files updated last. Zero downtime, old key confirmed genuinely dead. All 15 items now closed.
